package com.example.zelqanepfe.model;

/** Operator notification types (docs/round2-contract.md §5.5). */
public enum NotificationType {
    EMERGENCY_APPROVAL_REQUIRED,
    EMERGENCY_BROADCAST,
    EMERGENCY_REFUSED,
    CAMPAIGN_APPROVAL_REQUIRED,
    SUPPORT_OFFLINE,
    ZONE_SATURATION
}

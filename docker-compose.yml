services:
  postgres:
    image: postgres:16-alpine
    container_name: tpub-postgres
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - tpub_postgres_data:/var/lib/postgresql/data
    networks:
      - tpub_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: tpub-pgadmin
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      PGADMIN_CONFIG_SERVER_MODE: "False"
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: "False"
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    volumes:
      - tpub_pgadmin_data:/var/lib/pgadmin
    networks:
      - tpub_network
    depends_on:
      postgres:
        condition: service_healthy

  # The backend reads its configuration from .env (env_file). JWT_SECRET is REQUIRED (≥ 32 bytes, never
  # the value once committed to git): without it the container stops at startup with a clear message.
  # Recommended as well: MEDIA_SIGNING_SECRET, TOTP_ENCRYPTION_KEY, TPUB_ADMIN_INITIAL_PASSWORD and
  # TPUB_TOTP_REQUIRED_ROLES=ADMINISTRATEUR,SUPERVISEUR,OPERATEUR (see .env.example).
  backend:
    build:
      context: ./BackEnd
      dockerfile: Dockerfile
    container_name: tpub-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SPRING_PROFILES_ACTIVE: docker
      DB_HOST: postgres
    ports:
      - "${SERVER_PORT:-8080}:8080"
    volumes:
      - tpub_uploads:/app/uploads
    networks:
      - tpub_network
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  tpub_postgres_data:
    name: tpub_postgres_data
  tpub_pgadmin_data:
    name: tpub_pgadmin_data
  tpub_uploads:
    name: tpub_uploads

networks:
  tpub_network:
    name: tpub_network
    driver: bridge
